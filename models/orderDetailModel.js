export default class OrderDetail {
    constructor(order_id,product_id,customer_id,quantity){
        this.order_id = order_id
        this.product_id = product_id
        this.customer_id = customer_id
        this.quantity = quantity
    }
}